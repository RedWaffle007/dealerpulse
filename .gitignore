# OS / editor cruft
.DS_Store
*.log
.~lock.*#

# Node / Next — dealerpulse/.gitignore already covers its subtree; this is a safety net
node_modules/
.next/

# Internal session handoff (delete this line if you want it in the repo)
HANDOFF.md

# Internal build brief (not part of the submission)
CODEX-BRIEF.md

# The company's own assignment brief - not ours to republish
ASSIGNMENT.md
